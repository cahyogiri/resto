<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar</title>
    <%- include('../templat/head'); %>
</head>
<body style="background: lightgray">

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12">

                <% if (messages.success) { %>
                    <div class="alert alert-success" role="alert"><%- messages.success %></div>
                <% } %>
                
                <% if (messages.error) { %>
                    <div class="alert alert-danger" role="alert"><%- messages.error %></div>
                <% } %>

                <div class="card border-0 shadow rounded">
                    <div class="card-body">
                        <h1 class="text-center">DAFTAR PENGUNJUNG</h1>
                        <a href="daftar/new" class="btn btn-md btn-success mb-3">TAMBAH</a>
                        <table class="table table-bordered">
                            <thead>
                              <tr class="text-center">
                                <th scope="col">#</th>
                                <th scope="col">Nomor Meja</th>
                                <th scope="col">Jumlah Orang</th>
                                <th scope="col">Waktu Datang</th>
                                <th scope="col">Pelayanan</th>
                                <th scope="col"></th>
                              </tr>
                            </thead>
                            <tbody>
                                <% if(data.length){
                                    
                                    for(var i = 0; i< data.length; i++) {%>  
                                        <tr class="text-center">
                                            <th scope="row"><%= (i+1) %></th>
                                            <td><%= data[i].nomor%></td>
                                            <td><%= data[i].jumlah%></td>
                                            <td><%= data[i].waktu%></td>
                                            <td><%= data[i].pelayanan%></td>
                                            <td class="text-center">
                                                <a href="/daftar/<%=data[i].id%>/edit" class="btn btn-sm btn-primary">EDIT</a>
                                                <a href="/daftar/<%=data[i].id%>/delete" class="btn btn-sm btn-danger">HAPUS</a>
                                            </td>
                                        </tr>
                                    <% }
                                    
                                }else{ %>
                                    <tr>
                                        <td colspan="4">
                                            <div class="alert alert-danger" role="alert">
                                                Data Tidak Ditemukan !
                                            </div>
                                        </td>
                                    </tr>
                                <% } %>    
                            </tbody>
                        </table>  
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../templat/body'); %>
</body>
</html>